name: dart_rtp_recorder
description: Dart-rtp-recorder is a lightweight Real-time Transfer Protocol (RTP) recorder of asterisk using Asterisk Rest Interface for External Media channel.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.0.5

# Add regular dependencies here.
dependencies:
  dotenv: ^4.2.0
  eloquent: ^3.2.1
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
